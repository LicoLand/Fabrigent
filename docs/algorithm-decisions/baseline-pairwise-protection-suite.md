# Algorithm Decision: Baseline Pairwise Protection Suite (Retired)

## Lifecycle

| Item | Value |
| --- | --- |
| Decision track | `ALGORITHM` |
| Decision ID | `ALG-baseline-pairwise-protection-suite` |
| Decision status | `RETIRED` |
| Definition status | `NOT-SPECIFIED` |
| Existing authority | Historical Candidate record only |
| Authority targets | None |
| Predecessor or successor | Superseded by `ALG-core-v1-hybrid-ake` and `ALG-core-v1-double-ratchet`. |

## Question and scope

The record formerly attempted to select one baseline asynchronous hybrid
establishment and session-ratchet composition.

## Retirement reason

Its established frame exposed only epoch, direction and counter coordinates
and therefore did not encode the DH ratchet transition claimed by its
Prototype. Its prekey, transcript and key-confirmation semantics also did not
close the exact Hybrid AKE and proof questions required for Core v1.

The construction, Profile identity, schemas, runtime grammar, corpus, resource
contract and generated bytes are withdrawn together. None is a compatibility
path, vector source, fallback or identifier available for reuse.

## Source-derived conformance material

None. Historical Candidate cases were removed with the construction.

## Decision outcome

The conclusion is retired. The successors must independently close the exact
AKE and ratchet Algorithms before any Message Field or active wire is admitted.

## Definition evidence

The definition status is `NOT-SPECIFIED`. This page records retirement only
and is not an Algorithm Prototype or active Protocol Line input.
